// Notification routes
//
// GET /notifications - Get all notifications for current user (sorted by time desc)
//
// Notifications are auto-created when:
// - Worker submits a task (notifies buyer)
// - Buyer approves submission (notifies worker)
// - Buyer rejects submission (notifies worker)
// - Admin approves withdrawal (notifies worker)

module.exports = {};
